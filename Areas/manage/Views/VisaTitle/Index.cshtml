@model IEnumerable<DTRS.Models.VisaTitleMaster>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.VisaTitle)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.VisaTitle)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.VisaId }) |
            @Html.ActionLink("Details", "Details", new { id=item.VisaId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.VisaId })
        </td>
    </tr>
}

</table>
