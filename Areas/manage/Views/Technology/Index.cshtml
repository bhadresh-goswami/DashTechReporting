@model IEnumerable<DTRS.Models.TechnologyMaster>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TechTitle)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TechTitle)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TechId }) |
            @Html.ActionLink("Details", "Details", new { id=item.TechId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TechId })
        </td>
    </tr>
}

</table>
